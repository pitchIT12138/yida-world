# 算法里的“抄近路”，终于不用靠脑补了。

基线版本：free-exploration-2026-09-15

## 整篇表达

在同一查询上逐次量距离、沿边走，再下降一层，让近路有可追溯的访问路径。

先在标题与原文短引后操作「这一跳，究竟省去了什么？」。拖动查询点，逐次检查搜索走过的路径。随后按“接着探索”回到对应原文中的其他交互；补充文字可收起，操作始终展示。完整原文保留全部段落与原图。

作者语气：原文保留；补充计算、示意与情景均在操作旁标注。

## 风格约定

- 配色：#183a35、#f2f5ed、#d98b51
- 字体：系统中文字体；指标等宽数字；窄屏指标整段换行。
- 材质与留白：每篇由其论点或叙事情境决定画面，操作反馈就在原文对应位置。
- 动作：仅操作驱动；即时反馈；键盘、触摸和复位等价可用。

## 设计决策

### 操作必须改变可见关系

在同一查询上逐次量距离、沿边走，再下降一层，让近路有可追溯的访问路径。

取舍：图示为解释论点服务，范围与缺失信息就地说明。

## 片段分工

### 这一跳，究竟省去了什么？

- 稳定 ID：layer-route
- 位置：hnsw-p2
- 形式：experience

表达任务：可见节点与实际距离缓存一一对应，搜索结果由图算法计算，不播放预制文字路径。

放在这里的理由：紧接对应原文：如果能让读者自己选一个查询位置，看搜索从入口开始，一层一层接近目标，这段解释就不只是一张静态图片了。这里展示的是层级搜索的概念，不等于完整算法的性能基准。

操作与反馈：拖动查询点，逐次检查搜索走过的路径

实现约定：当前会话创作；隔离沙箱内直接操作，输入驱动实际图形与计算。

验收：

- #finish → 结束：最近节点 80，距离 4
- #query → 第1步 · 高层 · 比较节点 0
- #finish → 结束：最近节点 10，距离 0
- #reset → 第1步 · 高层 · 比较节点 0
- #query → 第1步 · 高层 · 比较节点 0
- #finish → 结束：最近节点 20，距离 0
- svg → 第1步 · 高层 · 比较节点 0
- #finish → 结束：最近节点 20，距离 1

代码指纹：1c73efaa8072a9dd0f03f9671391f94cbd7e7438ad0f7804d793e2a0ad8cf697

## 延续与修订

未来模型读取该基线和当前产物。用户的新意图优先；局部修改保留其他片段与整篇风格。改变约定时提升版本并说明取舍。重新验收和运行 npm run baselines 后，历史快照追加保存，已有快照不覆盖。
