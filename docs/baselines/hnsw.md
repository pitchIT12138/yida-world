# 算法里的“抄近路”，终于不用靠脑补了。

基线版本：explanation-v4.2026-09-14

## 整篇表达

在显式小图上逐步计算距离，比较分层路径和线性扫描的访问量。

选择查询位置 → 高层找方向 → 低层细找 → 对照扫描及近似边界。

作者语气：保留原文语气，补充内容使用可核对的解释。

## 风格约定

- 配色：#745788、#f5eff9、#243b3b
- 字体：正文15px，数字使用表格数字，标题18–20px；窄屏保持标签与完整数值。
- 材质与留白：图示随正文展开，细线区分论点，控制区与结果在同一阅读位置。
- 动作：操作即时响应；动画由读者启动，可停止或复位。

## 设计决策

### 围绕问题安排操作

在显式小图上逐步计算距离，比较分层路径和线性扫描的访问量。

取舍：减少无信息增量的装饰，保留能比较、计算和定位证据的反馈。

### 保留事实边界

原文引述、补充计算和情景假设分别说明；缺失资料不补成确定事实。

取舍：对缺少明细的地方仅提供已有数据范围内的比较。

## 片段分工

### 每一步为什么向这个点走

- 稳定 ID：hnsw-route
- 位置：hnsw-p1
- 形式：figure

表达任务：在一维教学图中选择查询点，逐步看候选距离、访问节点及层级下降。

放在这里的理由：此处原文刚提出“每一步为什么向这个点走”涉及的关系，读者可以就地核对，再继续阅读。

操作与反馈：在一维教学图中选择查询点，逐步看候选距离、访问节点及层级下降。

实现约定：显式一维教学图，查询距离缓存到 Map，每个节点只求值一次。逐步显示候选、移动与层级下降，不作为真实 HNSW 基准。

验收：

- 查询 76；结果 80；距离 4
- 第 1 步：从上层入口0开始
- 查询 10；结果 10；距离 0
- 第 1 步：从上层入口0开始

代码指纹：e84a8f8d39217743f19aeb02bacd076ca915cb4ed9dbab81966223c9f62014f0

## 延续与修订

未来模型读取该基线和当前产物。用户的新意图优先；局部修改保留其他片段与整篇风格。改变约定时提升版本并说明取舍。重新验收和运行 npm run baselines 后，历史快照追加保存，已有快照不覆盖。
