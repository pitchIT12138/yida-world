# 请求没有变少，为什么一批发送会更快？

基线版本：explanation-v4.2026-09-14

## 整篇表达

将同一工作量的时间拆成往返和处理，并检验批次及结果依赖带来的边界。

成本拆分 → 参数对照 → 批次和依赖。

作者语气：保留原文语气，补充内容使用可核对的解释。

## 风格约定

- 配色：#286785、#edf5f9、#243b3b
- 字体：正文15px，数字使用表格数字，标题18–20px；窄屏保持标签与完整数值。
- 材质与留白：图示随正文展开，细线区分论点，控制区与结果在同一阅读位置。
- 动作：操作即时响应；动画由读者启动，可停止或复位。

## 设计决策

### 围绕问题安排操作

将同一工作量的时间拆成往返和处理，并检验批次及结果依赖带来的边界。

取舍：减少无信息增量的装饰，保留能比较、计算和定位证据的反馈。

### 保留事实边界

原文引述、补充计算和情景假设分别说明；缺失资料不补成确定事实。

取舍：对缺少明细的地方仅提供已有数据范围内的比较。

## 片段分工

### 节省的是等待，不是工作量

- 稳定 ID：redis-compare
- 位置：redis-p1
- 形式：figure

表达任务：改变命令数、往返和处理时间，使用同一时间刻度对照耗时。

放在这里的理由：此处原文刚提出“节省的是等待，不是工作量”涉及的关系，读者可以就地核对，再继续阅读。

操作与反馈：改变命令数、往返和处理时间，使用同一时间刻度对照耗时。

实现约定：本地原生 HTML/CSS/JavaScript。数值由可见参数计算；示例、原文与核验说明分开呈现。

验收：

- 节省 0 ms；处理量均为 2 ms
- 节省 400 ms；处理量均为 12 ms

代码指纹：c036527446a19bfdefbae21a813bfdfe7c68887eb059795cebe0638e7e5bc483

### 如果下一条命令依赖上一条结果

- 稳定 ID：redis-boundary
- 位置：redis-p2
- 形式：figure

表达任务：比较多批次往返；勾选逐条依赖，观察为什么不能合成一批。

放在这里的理由：此处原文刚提出“如果下一条命令依赖上一条结果”涉及的关系，读者可以就地核对，再继续阅读。

操作与反馈：比较多批次往返；勾选逐条依赖，观察为什么不能合成一批。

实现约定：本地原生 HTML/CSS/JavaScript。数值由可见参数计算；示例、原文与核验说明分开呈现。

验收：

- 3 次往返；每批最多 2 条
- 492
- 92

代码指纹：a50b4faaef2aa5c8de751c390672ff46169ad55ae8baa8156f051e348e00c738

## 延续与修订

未来模型读取该基线和当前产物。用户的新意图优先；局部修改保留其他片段与整篇风格。改变约定时提升版本并说明取舍。重新验收和运行 npm run baselines 后，历史快照追加保存，已有快照不覆盖。
