# 请求没有变少，为什么一批发送会更快？

基线版本：free-exploration-2026-09-15

## 整篇表达

让每一段网络等待占据时间轴，改变批次后只压缩等待；存在前后依赖时禁止提前合批。

先在标题与原文短引后操作「命令还在，消失的是空等」。改变请求数和每批数量，一批一批推进。随后按“接着探索”回到对应原文中的其他交互；补充文字可收起，操作始终展示。完整原文保留全部段落与原图。

作者语气：原文保留；补充计算、示意与情景均在操作旁标注。

## 风格约定

- 配色：#183a35、#f2f5ed、#d98b51
- 字体：系统中文字体；指标等宽数字；窄屏指标整段换行。
- 材质与留白：每篇由其论点或叙事情境决定画面，操作反馈就在原文对应位置。
- 动作：仅操作驱动；即时反馈；键盘、触摸和复位等价可用。

## 设计决策

### 操作必须改变可见关系

让每一段网络等待占据时间轴，改变批次后只压缩等待；存在前后依赖时禁止提前合批。

取舍：图示为解释论点服务，范围与缺失信息就地说明。

## 片段分工

### 命令还在，消失的是空等

- 稳定 ID：network-tracks
- 位置：redis-p1
- 形式：experience

表达任务：批处理改变等待段数量，不改变处理总量；时间游标对应已完成命令数量。

放在这里的理由：紧接对应原文：Pipeline 的核心不是让命令凭空消失，而是把多条命令先发出去，再读取响应。简化示意下，逐条等待约花 6×(80+2) 毫秒，一批发送约花 80+6×2 毫秒。

操作与反馈：改变请求数和每批数量，一批一批推进

实现约定：当前会话创作；隔离沙箱内直接操作，输入驱动实际图形与计算。

验收：

- #reset → 逐条 492 ms / 当前 92 ms · 处理均为12 ms
- #count → 逐条 410 ms / 当前 90 ms · 处理均为10 ms
- #batch → 逐条 410 ms / 当前 170 ms · 处理均为10 ms
- #step → 已完成 3 / 5 条 · 86 ms
- #step → 已完成 5 / 5 条 · 170 ms
- #dependent → 逐条 410 ms / 当前 410 ms · 处理均为10 ms

代码指纹：bc43b30673d2ef2869d1a7f8672147a4c8a23f9058516015c2d23ee50fbc98f5

## 延续与修订

未来模型读取该基线和当前产物。用户的新意图优先；局部修改保留其他片段与整篇风格。改变约定时提升版本并说明取舍。重新验收和运行 npm run baselines 后，历史快照追加保存，已有快照不覆盖。
