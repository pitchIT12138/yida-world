# 一答一世界：实现边界

用户于 2026-09-06 修正了示例生产方式，并随后明确要求“先使用自己来作为 agent 进行默认的生成”。最新授权优先。

- 当前 Codex 会话可以担任离线内容生成 Agent：先读取原文，只输出代码视觉与交互，使用统一校验和 Runtime 保存默认产物。
- 此路径记录 method=codex、tier=session，不伪装为 API 调用，不虚构供应商模型 ID、费用或调用耗时。
- 下方“必须走 API”和“开发者不得生成”的约束，对此次用户明确授权的会话生成路径不适用。仍禁止在宿主内嵌入题材模板或使用预制位图替代 Agent 产物。

- 开发者 Agent 只实现通用宿主、模型调用、运行容器、校验和有限工具。
- 禁止由开发者手写各题材最终交互，再标记为产品 Agent 生成。
- 禁止由开发者提前生成相机、火星、桌面等场景素材来替产品 Agent 完成设计。
- 示例输入可以提前准备。示例输出必须经由产品自己的加工链路：读取原文、模型创作、校验／修复、保存产物。
- 离线精选和线上实时必须调用同一生成入口；区别只是配置的模型、迭代次数与是否保存为精选。
- 生成记录由服务端填写真实 runId、原文指纹、模型与时间。不得伪造模型调用、素材生成或验收记录。
- 模型未配置时保留原文，明确显示尚未加工。不得用模拟结果或手写示例掩盖缺失的模型调用。
- 已确认首版只使用文章上下文、代码生成和运行反馈，不接独立图片生成 API，不得依赖当前开发会话独有的工具。
- 宿主不能依据题材选择固定相机、账本、图表、火星等组件。生成的交互全部由统一 iframe 执行。
- 禁止在业务项目中引入多 Agent 框架或题材组件市场。

## 2026-09-07：整篇编排与持续设计基线

用户进一步要求：多种交互自然融入正文；回放能看清设计依据；持续沉淀基线以延续跨模型风格和实现。

后续修改先读取 `docs/baselines/README.md`、`src/lib/design-policy.ts` 和对应文章的基线，连同原文与当前代码一起理解。不要把整篇回答缩成一个孤立大组件，也不要机械地给所有文章增加相同数量的小组件。新增或修改生成片段时同步维护 design 记录，验收提升后执行 npm run baselines。用户新要求优先于现有基线。

## 2026-09-14：内部工作台与交互实现复用

用户明确授权独立 `/studio/` 本地工作台，以及“经验 + 可改编组件库”。本要求扩展此前禁止组件库的约定：允许沉淀通用交互代码并复用、参数调整、派生版本；仍不得在宿主按题材硬编码选用模板。

后续处理用户素材先阅读 `docs/studio.md`，通过 `npm run studio -- export --id ...` 获取当前原文、备注、历史和相关库条目。当前备注与事实优先；检查组件的适用条件，记录 libraryReferences，不把示例数据当新文章事实。通过统一校验和实际浏览器操作后，由助手直接纳入精选，无需逐篇再请求用户确认。内部备注、草稿、未入选素材不发布。入库需保留版本与验收证据，完成精选后同步基线。


## 2026-09-14：原图比较与素材清洗

用户明确授权用已保存原文图片重构交互。允许通过 SourceAsset 保存原图、由统一 Runtime 注入沙箱；不把图片字节写入生成代码，不开放 iframe 联网。素材清洗必须保留快照、差异理由、段落映射与备注，通过 Studio 版本校验保存。参见 docs/rebuild-2026-09-14.md。
